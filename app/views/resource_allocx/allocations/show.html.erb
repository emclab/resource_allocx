<% if @view_in_config && @erb_code %>
  <%= render inline: ERB.new(@erb_code).result(binding) %>
  
  <% else %>

<h4><%=@title %></h4>

<p>
  <b><%= t('Description') %></b>
  <%= @allocation.description %>
</p>
<p>
  <b><%=t('Engine')%>:</b>
  <%= SwModuleInfox::ModuleInfo.find_by_id(@allocation.detailed_resource_id).name %>
</p>
<p>
  <b><%= t('Start Date') %></b>
  <%= @allocation.start_date.strftime('%Y/%d/%d') if @allocation.start_date.present? %>
</p>
<p>
  <b><%= t('End Date') %></b>
  <%= @allocation.end_date.strftime('%Y/%d/%d') if @allocation.end_date.present? %>
</p>
<p>
  <b><%= t('Active') %></b>
  <%= @allocation.active ? t('Yes') : t('No') %>
</p>
<p>
  <b><%= t('Last Updated By') %></b>
  <%= @allocation.last_updated_by.name %>
</p>


  <%= link_to t('Back'), SUBURI + "/view_handler?index=0", :id => 'close', :class => BUTTONS_CLS['action'] %>
  &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
  <%= link_to t('Delete'), allocation_path(@allocation), :method => :delete, :confirm => I18n.t('Delete the record?'), :class => BUTTONS_CLS['action'] if has_action_right?('destroy', params[:controller], @allocation) %>
  &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
  <%= link_to t('Delete'), allocation_path(@allocation, :subation => @allocation.detailed_resource_category), :method => :delete, :confirm => I18n.t('Delete the record?'), :class => BUTTONS_CLS['action'] if has_action_right?('destroy_' + @allocation.detailed_resource_category, params[:controller], @allocation) %>


<% end %>
