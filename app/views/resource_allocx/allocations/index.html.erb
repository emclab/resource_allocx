<div id="main_body">
	<h4><%= @title %></h4>
	<% if @erb_code  %>	
		<%= render inline: ERB.new(@erb_code).result(binding) %>
	<% else %>
		<%= render partial: 'form_list', locals: {models: @alloc} %>
	<% end %>
</div>

